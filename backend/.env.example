# Environment variables for LeetCode Capturer Backend

# MongoDB connection string
# Get free MongoDB at: https://cloud.mongodb.com/
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/leetcode_capturer

# API Key for Chrome Extension authentication
# Generate a strong random key and store this same key in your Chrome extension
API_KEY=your_super_secret_api_key_here_change_this

# Port (optional - will use 3000 by default)
PORT=3000

# Node environment
NODE_ENV=production
